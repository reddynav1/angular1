name: Angular Project

on:
    push:
        branches:
            - main
jobs:
    build: 
        runs-on: self-hosted
        
        steps:
        - name: checkout repo
        uses: actions/checkout/@v1
        
        - name: Install dependencies
        run: npm install 
        
        - name: Build project
        run: ng build --prod
    
